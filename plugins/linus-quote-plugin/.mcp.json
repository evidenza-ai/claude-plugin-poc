{
    "mcpServers": {
      "evidenza-linus-quote": {
        "type": "http",
        "url": "${EVIDENZA_MCP_URL}",
        "oauth": {
            "clientId": "${EVIDENZA_OAUTH_CLIENT_ID}"
        }
      }
    }
  }